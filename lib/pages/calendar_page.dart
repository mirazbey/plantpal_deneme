// lib/pages/calendar_page.dart (SADELEŞTİRİLMİŞ)

import 'package:flutter/material.dart';

class CalendarPage extends StatelessWidget {
  const CalendarPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Scaffold ve AppBar kaldırıldı, sadece içerik döndürülüyor.
    return const Center(
      child: Text(
        'Genel Bakım Takvimi Burada Olacak',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}